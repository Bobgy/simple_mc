1. obj格式导入和显示（朱稼乐准备写了）
2*. 立方体面的贴图和材质处理（面的标号最好与vec.h中FACE数组相对应），要注意贴图的方向。 //已经初步完成
3*. 键盘按键处理，glut自带的不支持多个键同时按，最好维护一个表表示当前某个键是否被按了。//已经完成
4*. 鼠标按键处理，支持按住破坏方块，选中的方块已经实现，见world.h中的block_and_face look_at_block(Vec3f p, Vec3f dir, flt r) const; //已经完成
5. 光照处理，（点光源和全局光照（太阳））写好生成光源的函数。（可以看下要求里面那些奇怪的名词是什么，有什么可以实现的） //预计写AO，龚源准备研究，目前很没头绪
6. GUI的绘制，绘制一条的物品栏，9个格子即可。 //可以参考MC
7. display list管理，目前是所有物体在同一个display list，有一个更新，全部更新会造成严重卡顿，需要分块放在display list里，做一个管理系统。

感觉物品栏的GUI+鼠标控制太麻烦了，还是不做了吧，就键盘123456789按键选中物品好了。

游戏场面的绘制我感觉分3部分实现比较好，静态场景（格子，用display list优化，有格子破坏/被摆放的时候重新生成），动态场景（人，或其他物体），
2D UI（FPS，物品栏，文字提示），每种单独写一个函数。